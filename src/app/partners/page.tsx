
import Layout from '@/src/components/layout/Layout';

const PartnersPage = () => {
    return (
        <Layout backgroundHeader="bg-black">
            <main className='h-screen'>Seja um parceiro</main>
        </Layout>
    );
};

export default PartnersPage;